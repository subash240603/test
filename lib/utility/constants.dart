import 'package:flutter/material.dart';

// Light-themed pastel colors inspired by the uploaded UI design
const primaryColor = Color.fromARGB(255, 66, 11, 231); // Purple pastel
const secondaryColor = Color.fromARGB(255, 45, 97, 114); // Orange pastel
const accentColor1 = Color.fromARGB(161, 7, 240, 197); // Teal pastel
const accentColor2 = Color(0xFFFF647C); // Coral pastel
const bgColor = Color.fromARGB(255, 207, 224, 190); // Light cream background
const cardColor =
    Color.fromARGB(255, 84, 63, 166); // White for cards and containersr
const textColor = Color.fromARGB(255, 0, 0, 0); // Dark gray for text
const defaultPadding = 16.0;

const MAIN_URL = 'http://localhost:3000';
